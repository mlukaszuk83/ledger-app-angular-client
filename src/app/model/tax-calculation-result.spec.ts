import { TaxCalculationResult } from './tax-calculation-result';

describe('TaxCalculationResult', () => {
  it('should create an instance', () => {
    expect(new TaxCalculationResult()).toBeTruthy();
  });
});
