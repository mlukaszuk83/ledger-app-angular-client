import { TaxCalculationParams } from './tax-calculation-params';

describe('TaxCalculationParams', () => {
  it('should create an instance', () => {
    expect(new TaxCalculationParams()).toBeTruthy();
  });
});
